{
  "id": "sidekick",
  "name": "Parley",
  "description": "Parley (formerly Sidekick) PWA backend \u2014 exposes the /v1/* OpenAI-Responses contract the parley proxy expects against any openclaw runtime. Plugin id stays 'sidekick' (legacy registration key referenced by installed openclaw configs).",
  "activation": {
    "onStartup": true
  },
  "enabledByDefault": true,
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "authToken": {
        "type": "string",
        "description": "Shared bearer token the parley proxy uses for HTTP auth. Optional; the proxy works without auth in dev."
      }
    }
  }
}
