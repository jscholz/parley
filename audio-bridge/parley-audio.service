[Unit]
Description=Parley audio bridge (WebRTC + STT) — parley-owned reference impl
After=network.target

[Service]
Type=simple
WorkingDirectory=%h/code/parley/audio-bridge
ExecStart=%h/code/parley/audio-bridge/.venv/bin/python bridge.py
# Single source of truth for app secrets (DEEPGRAM_API_KEY etc.) is the
# proxy's .env. The audio-bridge reads the same file so the proxy and
# the bridge can never drift on the same key. Keep .env at mode 600.
EnvironmentFile=%h/code/parley/.env
Environment=PARLEY_AUDIO_HOST=127.0.0.1
Environment=PARLEY_AUDIO_PORT=8643
Environment=PARLEY_PROXY_URL=http://127.0.0.1:3001
Restart=on-failure
RestartSec=3
StandardOutput=journal
StandardError=journal

[Install]
WantedBy=default.target
